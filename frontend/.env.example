# Optional: call the API directly (skips Vite proxy). Example: http://localhost:5000
# VITE_API_BASE_URL=http://localhost:5000

# Where Vite should proxy /api in dev (default in vite.config.js is http://127.0.0.1:5000)
# VITE_PROXY_TARGET=http://127.0.0.1:5000
